<nav class="navbar navbar-expand fixed-top bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <div class="d-flex justify-content-start">
      <a class="navbar-brand" href="#">WaterMelon</a>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to t("projects"), projects_path, class: "nav-link active"%>
        </li>
        <li class="nav-item">
          <%= link_to t("documents"), documents_path, class: "nav-link active"%>
        </li>
        <% if can? :access, :rails_admin %>
          <li class="nav-item">
            <%= link_to t("administration"), rails_admin.dashboard_path, class: "nav-link active"%>
          </li>
        <% end %>
      </ul>
    </div>
    <div class="d-flex justify-content-end">
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <%= link_to current_user.full_name.blank? ? "#{current_user.email}" : "#{current_user.full_name}", "#", class: "nav-link active dropdown-toggle", role: "button", "data-bs-toggle": "dropdown", "aria-expanded": "false" %>
          <ul class="dropdown-menu dropdown-menu-end p-2 rounded-3 mx-0 border-0 shadow" data-bs-theme="dark">
            <li><%= link_to "Profile", user_path(current_user), class: "dropdown-item rounded-2" %></li>
            <li><hr class="dropdown-divider"></li>
            <li><%= link_to "Logout", destroy_user_session_path, class: "dropdown-item rounded-2", data: { "turbo-method": :delete, turbo_confirm: 'Are you sure?'} %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>